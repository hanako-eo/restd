[package]
name = "stability"
version = "0.0.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
syn = { version = "2.0", features = ["full"] }

[dev-dependencies]
trybuild = { path = "../trybuild" }
macrotest = "1.0"
